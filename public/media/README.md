# Media handoff

Place replaceable local posters and video files under this directory when preparing a local build.
Set `VITE_MEDIA_BASE_URL` to the deployed media origin or path; application code resolves all configured paths through that value.

No project media is included in this repository. Empty content values intentionally render a local placeholder state instead of requesting a missing file.
