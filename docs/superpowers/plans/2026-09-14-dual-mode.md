# 双模式作品集实施计划

> **For agentic workers:** Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 首页简约版与 /p5/ 隐藏模式可往返，保留各自排版、字体、素材和动效。

**Architecture:** Vite 多页入口，两份独立 App 与 CSS 在不同文档运行。共享模式入口组件；相同媒体复用，不同媒体通过 P5 映射表隔离，不改变原始工作树。

**Tech Stack:** React 19、TypeScript、Vite 7、CSS、现有 GSAP。

**Spec:** 用户已在本任务确认：首页简约版、面具图标解锁 P5、短撕纸转场、独立入口按需加载。

## Global Constraints

- 新分支 codex/dual-mode-20260914，基于简约版 59cded2；P5 导入指定工作目录的实际文件，包括未提交实现。
- 不改原两个工作目录。原视频保持。不同 Hero 文件及其轮廓一一对应。
- 默认首页简约版，不自动改访客模式；/p5/ 可直接分享与刷新。
- 不添加运行依赖；使用系统字体；减少动态直接导航。实际链接支持新标签页和禁用 JS。
- 事实以用户最新说明为准；P5 内容修正不改变其布局。

### Task 1: 独立 P5 页面与媒体隔离

**Files:** 新增 src/p5/（源 P5 的 src 快照）、p5/index.html；修改 vite.config.ts；新增 public/media/p5/ 中实际冲突媒体。

- [x] 建立新工作树、安装锁文件依赖，记录来源。
- [x] 比较两份 public 文件 SHA256，复用相同文件，只隔离冲突内容。
- [x] 配置 build.rollupOptions.input 为 index.html 与 p5/index.html。
- [x] P5 lib/media.ts 对冲突媒体映射到 p5/ 前缀，HeroComposite 测试读取该专用视频。
- [x] 构建检查两个 HTML 的 CSS 依赖不串入对方主题，两个 Hero 哈希正确。

### Task 2: 面具入口及转场

**Files:** 新增 src/features/mode/ModeSwitch.tsx、ModeSwitch.css、ModeSwitch.test.tsx；修改两份 App.tsx。

- [x] 先写测试：默认模式链接 /p5/、P5 返回 /、点击后导航延迟、减少动态立即导航、Ctrl 点击保持浏览器原生行为、卸载清理。
- [x] 运行测试观察缺失实现失败。
- [x] 实现真实链接 + 黑白 SVG 面具；普通点击通过约400ms遮罩转场后导航。使用可注入 navigate 回调验证，不模拟浏览器导航。
- [x] 移动端44px触区；播放器/相册/开场时隐藏入口，避免遮挡操作。
- [x] 目标页面短入场揭幕，不重复展示简约版 HELLO；浏览器后退时恢复可交互状态。
- [x] 重跑测试。

### Task 3: 集成验证及交付

**Files:** PROJECT.md、README.md、scripts/prepare-pages.mjs（只在多页需要时）、.agent-state/STATUS.md。

- [x] 核对P5求职联系、短片年份及投放素材口径，不改变布局。
- [x] npm run check:media、npm run test:run、npm run typecheck、npm run lint、npm run build、git diff --check。
- [x] 启动新端口预览；HTTP核对 / 与 /p5/、主题CSS、专用视频。
- [ ] Hermes健康检查后验收桌面/手机、往返、浏览器历史、滚动与播放器；桥接失败如实记录。
- [x] 检查源P5与源简约版未被改写。给用户预览；线上发布沿用已授权范围，未验证项明确列出。
- [ ] 简约Hero线上静止仍需定位：已确认源文件完整，Range差异不能直接判为原因；不随整合盲改。

执行记录：本轮449项测试及媒体、类型、lint、构建通过。Hermes标签状态不一致，实际视觉未验收；本轮按用户“尝试”要求交付4187本地试做，未替换生产站点。详情见docs/dual-mode-review.md。
