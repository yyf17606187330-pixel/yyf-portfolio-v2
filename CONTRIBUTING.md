# 协作与 GitHub 流程

## 分支规则

- `main` 保持可运行，不直接在上面开发功能
- 一个 Issue 对应一个独立分支、一个独立工作树和一个 PR
- Codex 从最新 `main` 建立 `codex/<issue>-<简短主题>`；已有的 `feature/...`、`fix/...`、`docs/...` 分支可以继续使用
- 开工后尽早建立 Draft PR，让范围、负责人和进度可见
- 未通过验收前不合并回 `main`

## 多 Agent 与工作树

- 四到五个长期任务可以放在不同 Codex 对话中，但每个对话必须绑定不同的 Git 工作树，不能只在同一目录里切换分支
- Git 工作树是本地目录，里面绑定的仍是普通 Git 分支；推送后，同一分支和提交会出现在 GitHub
- 主／集成 Agent 统一处理 `App.tsx`、全局样式、依赖和跨模块接口；其他 Agent 不同时修改这些共享文件
- 独立审核可以使用临时审核对话读取 PR，不需要再占用一个开发分支

## Agent 开工声明

每个任务先在 Issue／PR 写四句话：

1. 我负责的目标
2. 我会修改的模块
3. 我明确不修改的模块
4. 我依赖的前置任务

若两个任务会修改同一共享文件，先由技术／集成负责人拆分接口或指定唯一修改人。

## PR 合并条件

- 目标、范围、非范围与实际修改一致
- 自动检查通过；已有基线故障必须明确关联单独 Issue
- 有桌面／手机截图或对应交互证据（纯文档 PR 可免）
- 产品／视觉变化由杨玉峰确认
- 技术冲突由技术负责人先解决；只有冲突改变产品或视觉方向时才交给杨玉峰裁决

创建 PR 的 Agent 负责补齐 PR 描述、关联 Issue 和验证证据。杨玉峰不需要代写技术记录，只需人工复核页面／变更、处理产品与视觉取舍，并决定是否合并。

默认使用 Squash Merge，让一个任务在 `main` 中对应一个清晰提交。

## 本地状态快照与跨设备接续

- 每个 Agent 在自己的工作树根目录覆盖维护 `.agent-state/STATUS.md`，作为当前有效状态，不写流水账
- 状态文件记录任务与分支、当前目标、已完成、涉及文件、未完成、验证、风险和下一步
- `.agent-state/` 只写入当前克隆的 `.git/info/exclude`，不进入提交、PR 或 GitHub
- 同一台电脑上的聊天压缩、暂停或新对话接手，只更新本地状态即可，不要求提交或推送
- 切换 Windows／macOS、需要远端备份或准备删除工作树时，才提交并推送当前最小一致状态
- 只有关键里程碑、阻塞、范围变化或正式交接需要更新 Draft PR

本地状态文件不能代替 `git status` 和实际差异。最终使用 Squash Merge，不会把必要的阶段性提交逐条带进 `main`。

## Windows、macOS 与 Linux CI

- 文本文件统一使用 LF；Windows 批处理和 PowerShell 文件可以保留 CRLF
- 文件名和 import 路径的大小写必须完全一致，避免只在 Linux CI 暴露错误
- Windows 与 macOS 都可以人工或通过浏览器 Agent 检查视觉；当前自动视觉脚本只兼容 macOS 浏览器路径
- `.DS_Store`、`Thumbs.db`、环境变量、本机配置、依赖和构建产物不得提交
- 跨平台 CI 合并并稳定后，再把它设为 `main` 的必需检查，避免在没有可靠检查时锁死仓库

## 什么时候更新 GitHub

- 开始任务
- 完成一个关键里程碑
- 出现阻塞或依赖变化
- 范围发生变化
- 交接给别人
- 验收完成

没有实质变化时不需要更新。

## 仓库内容边界

适合提交：

- 已确认的目标、范围、设计原则和修改决定
- Agent 责任边界、依赖关系、验收条件和交接记录
- 参考网站链接，以及“借鉴什么、不复制什么”的说明
- 素材命名、比例、格式和交付清单
- 与一个 Issue 直接相关的代码、测试和必要验收证据

不适合提交：

- 原始聊天全文、内部推理、Token 消耗和工具流水日志
- 验证码、访问令牌、账号密码或其他凭证
- 本机临时地址、个人绝对路径和只对一台电脑有效的状态
- 没有变化的日报、重复进度和未形成决定的随手想法
- 未获授权的第三方源码、Logo、字体、图片或视频
- 大型视频原片、全片和无长期价值的废弃方向稿

废弃方案只保留一条简短的“舍弃决定与原因”，不保存全部试错过程。真实视频通过外部媒体地址管理，Git 仓库只保存内容清单和路径规则。
