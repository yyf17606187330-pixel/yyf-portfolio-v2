# 杨玉峰个人作品集｜项目总纲

这是项目当前的“单一事实源”。方向变化时先更新本文件，再安排设计或开发任务。

## 1. 核心目标

- 最终目标：做成一个具有鲜明个人色彩、同时承载视频与图片、整体具有高级感的个人介绍与作品页面
- 定位：`杨玉峰｜影像导演 × AI 视觉创作者`
- 目标用户：潜在客户、合作方与招聘方
- 首要体验：`HELLO. → YANG YUFENG → 直接看作品`
- 判断标准：访客进入后能迅速知道“你是谁、你会什么、作品怎么样、如何联系”

这里的“高级感”不等于堆叠特效，而是来自高质量素材、清楚的层级、克制的版式、统一的动效和完整的细节。

## 2. 已确认范围

- 首页以作品为主：最终支持 10–15 个项目；当前原型为 3 个重点项目 + 9 个索引项目
- 四类作品：影像、AI 视频、摄影、设计与交互
- 同时支持视频与图片，横屏、竖屏作品都应获得合适的展示空间
- 全屏导航集中展示身份、能力、About 与联系方式
- 点击作品打开全屏播放器，不建立案例详情页
- 真实素材缺失时使用明确占位，不虚构成绩、客户或数据
- 当前不做：CMS、统计、域名、正式部署、社交平台矩阵

## 3. 项目阶段

1. **目标与约束**：确认最终目标、页面范围、视觉方向和协作规则
2. **内容准备**：整理真实作品、肖像、简介、邮箱与微信二维码
3. **视觉定稿**：结合真实素材确认色彩、字体、版式和媒体构图
4. **动效与交互**：完善全局光标流体、菜单和播放器转场
5. **集成与验收**：完成桌面、手机、性能、可访问性和真实内容检查
6. **发布准备**：在内容和视觉通过后，再处理媒体托管、域名与正式部署

当前已有一套可运行的技术原型和占位内容，但产品约束、真实内容与视觉定稿仍处于第 1 阶段。现有代码不是最终视觉，也不应因素材尚未到位而删除占位。本阶段的文档 PR 只整理 Markdown，不修改前端代码。

## 4. 开发方法：纵向切片

不先把所有页面层做完，再到最后接高级交互。先做一条从开场到播放的最小完整链路：

`HELLO. → 首页骨架 → 一个作品 → 导航菜单 → 播放器 → 基础转场／流体 → 手机与减少动态降级`

这条链路通过后，再批量增加作品、真实图片视频、完整视觉系统和细节优化。如果切片在流体、转场、移动端或性能上卡住，先解决风险，不继续扩大内容范围。每个合并到 `main` 的 PR 都应留下一个可运行、可检查的增量。

## 5. 四个责任角色

| 角色 | 当前负责人 | 负责 | 不负责 |
| --- | --- | --- | --- |
| 产品负责人 | 杨玉峰 | 目标用户、内容范围、优先级、最终取舍 | 具体技术实现 |
| 视觉负责人 | 杨玉峰 | 视觉方向、动效感受、截图验收 | 合并代码冲突 |
| 技术／集成负责人 | 主 Agent | 架构、共享文件、依赖关系、集成与 PR 准备 | 替用户决定审美方向 |
| QA／审核负责人 | 独立审核 Agent 或人工复核 | 测试、响应式、性能、可访问性证据 | 与功能负责人重复开发 |

同一个人可以兼任多个角色，但每个任务仍只能有一个直接负责人。

## 6. 文档优先级

1. `PROJECT.md`：当前有效的目标、范围、阶段和最高级约束
2. `docs/design-direction.md`：当前有效的视觉与动效方向
3. `docs/decision-log.md`：记录决定为何改变；它不能单独覆盖 `PROJECT.md`
4. `docs/superpowers/specs/portfolio-v2-spec.md`：产品与技术规格
5. `docs/superpowers/plans/`：执行步骤；与上级文档冲突时必须先更新计划

方向改变时，应在同一个 PR 同时更新 `PROJECT.md`、相关方向文档和决定记录，避免出现两个版本的事实。

## 7. 每个任务必须写清楚

- 目标：最终要改变什么
- 负责人：谁对结果负责
- 范围：允许修改哪些模块／文件
- 非范围：明确不做什么
- 依赖：开始前需要谁先完成
- 验收：什么证据说明完成

每个 Agent 开工前必须声明“我负责什么、我不负责什么”。共享入口文件与全局样式由技术／集成负责人统一处理。

## 8. 验收与记录

- 产品与视觉：由杨玉峰看实际页面和截图决定是否通过
- 技术：`test`、`typecheck`、`lint`、`build` 通过
- 交互：桌面与手机实际检查，无溢出、关键操作可用
- 记录：只在开工、关键完成、阻塞、范围改变、交接、完成时更新 Issue／PR；不要求为了“日报”每天刷记录

文档入口见 [docs/README.md](docs/README.md)，设计方向见 [docs/design-direction.md](docs/design-direction.md)，Agent 拆分见 [docs/agent-work-map.md](docs/agent-work-map.md)，阶段门槛见 [项目进度书](docs/superpowers/plans/2026-08-26-project-progress-gates.md)，分支与合并规则见 [CONTRIBUTING.md](CONTRIBUTING.md)。
