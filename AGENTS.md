# 仓库工作规则

适用于整个仓库；更近的 AGENTS.md 优先。用户在当前任务中的明确要求优先于旧项目说明。

## 开工与范围

1. 先确认项目根目录、当前分支、文件列表、git status 和实际差异；存在 .agent-state/STATUS.md 时先读当前快照。
2. 读取 PROJECT.md，以及与任务直接有关的设计或素材说明。对应 Issue 存在时一并核对。
3. 修改前说明目标、实际相关文件、判断依据、范围、依赖、验收和风险。大范围改动先取得用户确认；已授权范围持续执行。
4. 只修改与目标相关的内容，不覆盖其他任务的未提交工作。
5. PROJECT.md 保存当前事实；不得虚构客户、成绩、任职、联系方式或作品完成程度。

## 分支与所有权

- 一个任务使用一个独立分支和工作树；新分支默认 codex/ 前缀，不直接在 main 开发。
- 不允许两个 Agent 共用工作目录或同时修改共享入口。
- 主控统一修改 App.tsx、main.tsx、全局样式、依赖和构建配置，并负责集成与验证。
- 内容负责 src/content/；视觉负责指定页面模块；动效负责指定交互；流体仅由指定负责人修改。
- 独立审核默认只读。任务重叠时先由主控划定文件边界。
- 尽早建立 Draft PR；创建者维护范围、实际修改、验证和待办。是否合并、部署由杨玉峰决定，默认 Squash Merge。

## 验证与报错

- 交付前按范围运行 npm run check:media、npm run test:run、npm run typecheck、npm run lint、npm run build。
- 页面变化必须实际检查桌面与手机、点击区域、滚动、全局样式影响和控制台；旧截图不能代替当前验收。
- 概括关键错误，判断是否由本次修改导致；修复后重跑同一检查，连续两次失败先复盘。
- 没有验证结果不宣称完成，未检查的浏览器、触屏真机或性能范围如实说明。
- 文件名与 import 大小写一致；文本使用 LF，不提交环境变量、依赖、构建产物或本机配置。

## 本机记录与交接

- 在工作树根目录覆盖维护 .agent-state/STATUS.md，仅保存目标、当前修改、完成、待办、验证、风险和下一步。
- .agent-state/ 写入当前克隆的 .git/info/exclude，保持本机可恢复，不进入提交或 PR。
- 同机暂停或上下文交接只更新快照；换设备、远端备份或删除工作树前才提交并推送最小一致状态。
- 原片及已使用媒体保持；新增网页衍生物按任务授权处理，MP4 通过 Git LFS 管理。
- 不记录原始聊天、凭证、验证码或工具流水。项目文档只维护当前有效内容，不累积历次阶段报告。

## 文档入口

- PROJECT.md：目标、真实内容、当前任务。
- docs/design-direction.md：当前视觉与交互标准。
- docs/content-inventory.md：媒体与事实归属。
- README.md：运行和检查方法；public/media/README.md：媒体目录规则。
