# 仓库级 Agent 工作规则

本文件适用于整个仓库。若任务目录中以后出现更具体的 `AGENTS.md`，以距离目标文件最近的规则为准。

## 1. 开工前

1. 用 `git rev-parse --show-toplevel` 确认项目根目录，并查看 `git status` 与当前文件列表。
2. 先读 `PROJECT.md`、`CONTRIBUTING.md`、对应 Issue，以及与任务直接相关的 `docs/` 文档。
3. 声明目标、负责范围、明确不做、依赖、验收标准和风险点。
4. 找到实际相关文件并说明判断依据；没有定位、计划和用户确认，不做大范围修改。

`PROJECT.md` 是项目事实源。占位内容是当前阶段的正常状态；不得为了“完整”而虚构客户、成绩、联系方式或作品资料。

## 2. 分支、工作树与 PR

- 一个 Issue／任务／Codex 对话只使用一个独立分支和一个独立工作树。
- Codex 新建分支默认命名为 `codex/<issue>-<简短主题>`；不要直接在 `main` 开发。
- 不允许两个 Agent 共用同一个工作目录，也不允许两个 Agent 同时修改共享入口。
- 尽早推送分支并建立 Draft PR。创建 PR 的 Agent 负责填写目标、范围、实际修改、验证结果和待办；杨玉峰只负责人工复核、产品／视觉取舍和是否合并。
- 默认 Squash Merge，因此功能分支可以保留便于交接的阶段性提交。

本地分支推送后会成为 GitHub 上可见的远端分支；工作树只是该本地分支的独立目录，不是另一套分支系统。

## 3. 所有权与并行边界

- `src/App.tsx`、`src/main.tsx`、`src/styles/index.css`、依赖和构建配置默认由主／集成 Agent 统一修改。
- 内容 Agent 负责 `src/content/` 和真实素材清单，不改页面结构。
- 作品／视觉 Agent 负责作品区和视觉提案；涉及全局样式时先交给主／集成 Agent 集成。
- 流体 Agent 只负责 `src/features/fluid/` 及其测试、降级和性能。
- 动效／交互 Agent 负责开场、导航和播放器；不得顺手修改流体或作品数据。
- 独立审核 Agent 只报告问题和证据，除非另开修复 Issue，否则不直接重做功能。

更完整的五路拆分见 `docs/agent-work-map.md`。

## 4. 本地工作树汇报（同机恢复）

每个 Agent 在自己的工作树根目录维护 `.agent-state/STATUS.md`。该目录必须写入当前克隆的 `.git/info/exclude`，只保留在本机，不进入提交或 PR。

- 开工时若状态文件存在，先读取它，再检查 `git status`、`git diff` 和最近提交。
- 聊天即将压缩、任务暂停或在同一台电脑交给新对话前，覆盖更新状态文件；不要追加成流水账。
- 状态文件只保存当前有效快照：任务与分支、当前目标、已完成、涉及文件、尚未完成、验证结果、已知风险和下一步。
- 代码与未提交修改继续保留在当前工作树；状态文件用于解释现状，不能代替查看实际差异。
- 仅为同机上下文恢复时，不要求创建 Git 提交、推送分支或更新 PR。

不要在本地汇报中记录原始聊天、内部推理、Token、验证码或凭证。

## 5. 跨设备、备份或删除工作树前

只有准备切换 Windows／macOS、需要远端备份，或将要删除当前工作树时，才建立 Git 检查点：

1. 提交当前最小一致状态；尚未完成但必须接续时，可以使用 `wip:` 提交。
2. 推送当前分支，确保另一台设备能够获取。
3. 仅在关键里程碑、阻塞、范围变化或正式交接时更新 Draft PR。
4. 接手 Agent 先读 PR／Issue 和提交，再结合自己工作树的本地状态继续。

本地 `.agent-state/` 不随 Git 同步。阶段性 `wip:` 提交在最终 Squash Merge 后不会污染 `main` 历史。

## 6. 验证与平台分工

- 代码任务按影响范围运行测试，并在交付前运行 `npm run test:run`、`npm run typecheck`、`npm run lint`、`npm run build`。
- Windows 与 macOS 都可以人工检查页面或通过浏览器 Agent 做视觉验收；当前 `qa:visual` 自动脚本只兼容 macOS 浏览器路径。
- 文件名与 import 的大小写必须完全一致；不要依赖 Windows／macOS 默认的大小写不敏感行为。
- 不提交 `.env`、系统生成文件、依赖目录、构建目录或本机配置。
- 报错必须记录关键错误并判断是否由本次修改导致；修复后重跑同一命令，连续两次失败则暂停复盘。

没有验证结果，不得宣称完成。
